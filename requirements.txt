# Runtime dependencies — installed in the Docker image (Cloud Run)
streamlit
pandas
numpy
plotly

# Removed (confirmed unused in production import chain):
#   requests  — only imported in src/data/acs_data.py which is never imported
#   us        — not imported anywhere in src/ or run_leaflet.py
#   openpyxl  — not imported anywhere in src/ or run_leaflet.py
#   python-dotenv — not imported anywhere in src/ or run_leaflet.py
#   folium, streamlit-folium — superseded by Leaflet.js implementation
#   cenpy     — Census API client only used in deleted pipeline scripts
#   matplotlib — Plotly is used instead; matplotlib never imported in src/
#   geopandas + transitive deps (fiona, pyproj, shapely) — only used by
#               scripts/update_data.py; see requirements-scripts.txt
